# Trading Trainer Project Instructions

## Git Workflow

After implementing and verifying every new feature, stage the relevant changes,
commit them to the `Dev` branch, and push the commit to the configured GitHub
remote. Do not include generated logs, local databases, credentials, or other
runtime artifacts in those commits.
